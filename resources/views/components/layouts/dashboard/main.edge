<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      {{ title || 'Admin Dashboard' }}
    </title>

    @vite(['resources/css/app.css', 'resources/js/app.js'])
  </head>
  <body class="bg-gray-100 font-sans">

    <div class="min-h-screen">
      <header class="bg-white shadow-sm border-b border-gray-200 sticky top-0 z-40">
        <nav class="container mx-auto px-6 py-3">
          <div class="flex items-center justify-between">

            {{-- Kiri: Logo & Navigasi Utama --}}
            <div class="flex items-center gap-10">
              <a href="#" class="text-gray-800 text-xl font-bold">Admin Quick</a>

              <ul class="hidden md:flex items-center space-x-8 text-gray-600">
                <li>
                  @navLink({
                    href: route('dashboard.index'),
                    active: [route('dashboard.index')],
                  })
                    Dashboard
                  @end
                </li>
                <li>
                  @navLink({
                  href: route('dashboard.products.index'),
                  active: [route('dashboard.products.index'), route('dashboard.products.create'), route('dashboard.products.edit', [1])],
                  })
                    Products
                  @end
                </li>
              </ul>
            </div>

            <div class="flex items-center gap-4 text-gray-800">
              <span class="font-medium">{{ auth.user.name }}</span>
              <form action="{{ route('logout') }}" method="POST">
                {{ csrfField() }}
              <button
                  type="submit"
                  class="bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded-md text-sm transition-colors"
                >
                  Logout
              </button>
              </form>
            </div>
          </div>
        </nav>
      </header>

      <main>
        {{{ await $slots.main() }}}
      </main>
    </div>

    <footer class="text-center text-sm text-gray-500 py-4 border-t bg-white">
      &copy; 2025 Quick Commerce. All Rights Reserved.
    </footer>

  </body>
</html>
